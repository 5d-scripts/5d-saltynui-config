_CONFIG = 
{
    focusUI = true, -- if set to true, the mouse cursor will be focused on the salty UI allowing you to click "copy link". because of that, the player is not able to move while the UI is open.
                    -- if set to false, this feature becomes unavailable but results in the player still being able to move.
    showUIOnMute = true,
    toggleRangeCircle = 
    {
        enabled = true,
        drawTimeSeconds = 2,
        color = vector4(214, 167, 66, 165),
        mutedColor = vector4(197, 26, 26, 165)
    }
}